import * as React from "react";
import { StatelessComponent } from "react";

const IndexPage: StatelessComponent = () => (
  <div>
    <p>Welcome to new site.</p>
  </div>
);

export default IndexPage;
